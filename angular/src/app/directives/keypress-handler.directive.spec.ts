import { KeypressHandlerDirective } from './keypress-handler.directive';

describe('KeypressHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new KeypressHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
